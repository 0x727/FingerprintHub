id: svnserve
info:
  name: Subversion
  author: nmap,cn-kali-team
  tags: detect,tech,svnserve,service
  severity: info
  metadata:
    rarity: 0
tcp:
- name: 'null'
  inputs:
  - data: ''
  host:
  - '{{Hostname}}'
  extractors:
  - name: svnserve
    type: regex
    regex:
    - '^\( success \( \d \d \( (?:ANONYMOUS )?\) \( '
