id: mailpit
info:
  name: mailpit
  author: cn-kali-team
  tags: detect,tech,mailpit
  severity: info
  metadata:
    fofa-query:
    - title="mailpit"
    product: mailpit
    shodan-query:
    - title:"mailpit"
    vendor: axllent
    verified: true
http:
- method: GET
  path:
  - '{{BaseURL}}/'
  matchers:
  - type: regex
    regex:
    - (?mi)<title[^>]*>mailpit.*?</title>
