id: ws_ftp_server
info:
  name: ws_ftp_server
  author: cn-kali-team
  tags: detect,tech,ws_ftp_server
  severity: info
  metadata:
    product: ws_ftp_server
    shodan-query:
    - title:"ad hoc transfer"
    vendor: progress
    verified: true
http:
- method: GET
  path:
  - '{{BaseURL}}/'
  matchers:
  - type: regex
    regex:
    - (?mi)<title[^>]*>ad hoc transfer.*?</title>
