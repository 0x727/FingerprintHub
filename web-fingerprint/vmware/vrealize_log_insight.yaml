id: vrealize_log_insight
info:
  name: vrealize_log_insight
  author: cn-kali-team
  tags: detect,tech,vrealize_log_insight
  severity: info
  metadata:
    fofa-query:
    - title="vrealize log insight"
    google-query:
    - intitle:"vrealize log insight"
    product: vrealize_log_insight
    shodan-query:
    - http.title:"vrealize log insight"
    vendor: vmware
    verified: true
http:
- method: GET
  path:
  - '{{BaseURL}}/'
  matchers:
  - type: regex
    regex:
    - (?mi)<title[^>]*>vrealize log insight.*?</title>
