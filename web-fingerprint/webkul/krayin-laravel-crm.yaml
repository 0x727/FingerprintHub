id: krayin-laravel-crm
info:
  name: krayin-laravel-crm
  author: cn-kali-team
  tags: detect,tech,krayin-laravel-crm
  severity: info
  metadata:
    fofa-query:
    - title="krayin" || header="krayin_crm_session"
    product: krayin-laravel-crm
    vendor: webkul
    verified: true
http:
- method: GET
  path:
  - '{{BaseURL}}/'
  matchers:
  - type: regex
    regex:
    - (?mi)<title[^>]*>krayin.*?</title>
