id: hertzbeat
info:
  name: hertzbeat
  author: cn-kali-team
  tags: detect,tech,hertzbeat
  severity: info
  metadata:
    fofa-query:
    - title="hertzbeat"
    product: hertzbeat
    shodan-query:
    - http.title:"hertzbeat"
    - title:"hertzbeat"
    vendor: apache
    verified: true
http:
- method: GET
  path:
  - '{{BaseURL}}/'
  matchers:
  - type: regex
    regex:
    - (?mi)<title[^>]*>hertzbeat.*?</title>
