id: cockpit
info:
  name: cockpit
  author: cn-kali-team
  tags: detect,tech,cockpit
  severity: info
  metadata:
    fofa-query:
    - title="cockpit"
    product: cockpit
    shodan-query:
    - title:"cockpit"
    vendor: cockpit-project
    verified: true
http:
- method: GET
  path:
  - '{{BaseURL}}/'
  matchers:
  - type: regex
    regex:
    - (?mi)<title[^>]*>cockpit.*?</title>
